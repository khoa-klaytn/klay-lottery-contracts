{
  "name": "lottery",
  "description": "A lottery system for SweepStakes",
  "main": "index.js",
  "version": "2.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:testnet": "HARDHAT_NETWORK=testnet hardhat run scripts/deploy.ts",
    "deploy:mainnet": "HARDHAT_NETWORK=mainnet hardhat run scripts/deploy.ts",
    "sync": "hardhat run scripts/sync.ts",
    "test": "HARDHAT_NETWORK=testnet hardhat test",
    "test:coverage": "hardhat coverage",
    "test:size-contracts": "hardhat size-contracts"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^4.2.0"
  },
  "dependencies": {
    "@bisonai/orakl-contracts": "1.0.0"
  }
}
